apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-book-flights
  namespace: mcp-book-flights
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-book-flights
  template:
    metadata:
      labels:
        app: mcp-book-flights
    spec:
      containers:
        - name: mcp-book-flights
          image: mcp-book-flights:latest
          imagePullPolicy: Never
          env:
            - name: BOOK_FLIGHTS_BASE_URL
              value: http://rest-book-flights.rest-book-flights.svc.cluster.local:8000
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "8000"
            - name: MCP_PATH
              value: /mcp
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-book-flights
  namespace: mcp-book-flights
spec:
  selector:
    app: mcp-book-flights
  ports:
    - name: mcp
      port: 8000
      targetPort: 8000
      appProtocol: agentgateway.dev/mcp
